# MM ownership and cut18 development screens

Image `04d1914c` with packed routing and owner-local MM towers. Fused packing, shared overlap and boundary ownership are disabled. Both partitions23,23 and18,28 passed40/40 reasoning API cases plus prefix and multimodal gates. Raw startup, runtime, container and loaded ownership receipts are archived.

Cut18 completed12/12 coding tasks naturally with static checks, no truncations:

| C | Median per-request decode tokens/s | Completed latency s |
|---:|---:|---:|
| 1 | 157.582 | 25.168 |
| 2 | 126.560 | 37.490 |
| 4 | 96.153 | 49.405 |

These are development measurements, not final qualification or tool-quality scores. Variable output lengths affect latency. Original placement JSONs include CPU-buffer-inflated memory estimates and are retained as historical diagnostics; use the later CUDA-only attestation/helper for placement decisions. Raw files are lossless gzip with exact hashes.
